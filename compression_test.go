package codeeval

/*
import (
	"testing"
)

func TestBurrowsWheelerDecomp(t *testing.T) {
	cases := []struct {
		in   string
		want string
	}{
		{"yyeep$ aass", "easy peasy$"},
		{"ooooio,io$Nnssshhhjo  ee  o  nnkkkkkkii ", "abc"},
		// {"oooooooo$  ffffffff     ffffffffuuuuuuuuaaaaaaaallllllllbbBbbBBb", "aaaaaaaaab"},
		// {"edarddddddddddntensr$  ehhhhhhhhhhhJ aeaaaaaaaaaaalhtf thmbfe           tcwohiahoJ eeec t e ", "bksxyz"},
	}
	for _, c := range cases {
		got := BurrowsWheelerDecomp(c.in)
		if got != c.want {
			t.Errorf("BurrowsWheelerDecomp(%v) == %v, want %v ",
				c.in, got, c.want)
		}
	}
}

func TestString2ArrSorted(t *testing.T) {
	cases := []struct {
		in   string
		want []rune
	}{
		{"yyeep$ aass", []rune{'a', 'b'}},
		{"ooooio,io$Nnssshhhjo  ee  o  nnkkkkkkii ", []rune{'a', 'b'}},
		// {"oooooooo$  ffffffff     ffffffffuuuuuuuuaaaaaaaallllllllbbBbbBBb", "aaaaaaaaab"},
		// {"edarddddddddddntensr$  ehhhhhhhhhhhJ aeaaaaaaaaaaalhtf thmbfe           tcwohiahoJ eeec t e ", "bksxyz"},
	}
	for _, c := range cases {
		got := String2ArrSorted(c.in)
		for pos, val := range c.want {
			if val != got[pos] {
				t.Errorf("String2ArrSorted(%v) == %v, want %v ",
					c.in, string(got), string(c.want))
			}
		}
	}
}
*/
